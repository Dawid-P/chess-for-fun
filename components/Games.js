import React from "react";

const Games = ({ data }) => {
  return (
    <ul>
      <h2></h2>
      {/* {data.map((item) => (
        <li key={item.url}>{item.url}</li>
      ))} */}
    </ul>
  );
};

export default Games;
